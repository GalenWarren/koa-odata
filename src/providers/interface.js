// the symbol for the get metadata function
export const getMetadata = Symbol();

// the symbol for the get data function
export const getData = Symbol();

// symbol for the process request function
export const processRequest = Symbol();
